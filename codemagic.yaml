workflows:
  react-native-android:
    name: React Native Android v5
    max_build_duration: 120
    instance_type: mac_mini_m2
    environment:
      android_signing:
        - shanyai-key
      # groups:
      #   - google_play
      vars:
        PACKAGE_NAME: "co.event.scanner"
    scripts:
      - name: Install npm dependencies
        script: |
          npm install --legacy-peer-deps
      - name: Run Expo Prebuild
        script: |
          npx expo prebuild --clean
      - name: Set Android SDK location
        script: |
          echo "sdk.dir=$ANDROID_SDK_ROOT" > "$CM_BUILD_DIR/android/local.properties"
      - name: Set up app/build.gradle
        script: |
          cp ./support-files/build.gradle android/app/
      - name: Build Android release
        script: |
          LATEST_GOOGLE_PLAY_BUILD_NUMBER=$(google-play get-latest-build-number --package-name "$PACKAGE_NAME")
          
          # If Google Play returns a number less than 20000, start from 20006 (v4's last version)
          if [ -z "$LATEST_GOOGLE_PLAY_BUILD_NUMBER" ] || [ "$LATEST_GOOGLE_PLAY_BUILD_NUMBER" -lt 20000 ]; then
              UPDATED_BUILD_NUMBER=20007
          else
              UPDATED_BUILD_NUMBER=$(($LATEST_GOOGLE_PLAY_BUILD_NUMBER + 1))
          fi
          
          # Extract patch version from build number (20007 -> 7)
          PATCH_VERSION=$((UPDATED_BUILD_NUMBER - 20000))
          
          echo "Building with versionCode=$UPDATED_BUILD_NUMBER and versionName=2.0.$PATCH_VERSION"
          
          cd android
          ./gradlew bundleRelease \
            -PversionCode=$UPDATED_BUILD_NUMBER \
            -PversionName=2.0.$PATCH_VERSION
    artifacts:
      - android/app/build/outputs/**/*.aab
    publishing:
      email:
        recipients:
          - user_1@example.com
          - user_2@example.com
        notify:
          success: true
          failure: false
      # google_play:
      #   credentials: jjkk
      #   track: internal
      #   submit_as_draft: true

  